; @microsoft/agent-governance-sdk@^3.7.0 pulls in @noble/*@2.2.0 which
; require Node >= 20.19.0. engine-strict makes npm hard-fail installs on
; older Node instead of just warning (TRUS-847, PR #9 review).
engine-strict=true
