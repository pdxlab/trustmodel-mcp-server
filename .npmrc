; Keep contributor installs aligned with package.json's Node.js floor.
; This project-level setting is not included in the published package, so
; consumers are also protected by the preinstall check and guarded launcher.
engine-strict=true
